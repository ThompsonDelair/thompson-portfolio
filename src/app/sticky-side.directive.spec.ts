import { StickySideDirective } from './sticky-side.directive';

describe('StickySideDirective', () => {
  it('should create an instance', () => {
    const directive = new StickySideDirective();
    expect(directive).toBeTruthy();
  });
});
